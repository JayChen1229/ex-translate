# Environment Variables

# Worker API URL (optional - defaults to production worker)
# For local development, uncomment and set to your local worker URL
# VITE_WORKER_URL=http://localhost:8787

# Leave empty for production (will use https://extranslator-worker.samolab.workers.dev)
